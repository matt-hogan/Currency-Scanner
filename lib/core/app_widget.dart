import 'package:camera/camera.dart';
import 'package:currencyscanner/pages/camera.dart';
import 'package:flutter/material.dart';
import 'package:flutter/services.dart';
import 'package:currencyscanner/pages/scanner.dart';

/* class AppWidget extends StatelessWidget {
  const AppWidget({Key? key, required this.cameras}) : super(key: key);

  final List<CameraDescription> cameras;
  @override
  Widget build(BuildContext context) {
    SystemChrome.setPreferredOrientations([
      DeviceOrientation.portraitUp,
      DeviceOrientation.portraitDown,
    ]);
    return MaterialApp(
      themeMode: ThemeMode.dark,
      title: "Currency Scanner",
      theme: ThemeData(
        brightness: Brightness.dark,
      ),
      home: SafeArea(child: Home(cameras)),
    );
  }
}

 */